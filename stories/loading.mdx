import { Story } from "@storybook/addon-docs";

# Main Menu

<Story id="cu-ng-design-library-components-loading--default" />

---

Example code

```javascript
<lib-loading *ngIf="loading"/>
```

## Implementations

Before a fetch request the component loading state should be set to true. When the fetch request is complete the component loading state should be set to false.



